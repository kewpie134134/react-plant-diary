import { makeStyles } from '@material-ui/core/styles';

export const useSelectImageStyles = makeStyles((theme) => ({
  root: {
    '& > *': {
      margin: theme.spacing(1),
    },
  },
  input: {
    display: 'none',
  },
}));
